import { createTheme } from '@mui/material';

export default createTheme({
  typography: {
    fontFamily: 'sans-serif',
    h1: {
      fontSize: '2rem',
      fontWeight: 700,
      color: 'red',
      letterSpacing: '-2%',
    },
    h2: {
      fontSize: '2rem',
      fontWeight: 700,
    },
  },
});
